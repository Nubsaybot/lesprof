
Ctrl-Shift-P -> CreateInvironment -> Venv создаем виртуальное пространство
Терминал -> Создать терминал
pip install openpyxl
pip install undetected_chromedriver
pip install bs4
python -m pip install nuitka устанавливаем компилятор
pip freeze проверяем
python -m nuitka --standalone --onefile MixaParser1.py компилируем